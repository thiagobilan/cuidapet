import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:cuida_pet/app/modules/main_page/main_page.dart';

main() {
  testWidgets('MainPagePage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(MainPagePage(title: 'MainPage')));
    //  final titleFinder = find.text('MainPage');
    //  expect(titleFinder, findsOneWidget);
  });
}
