import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:cuida_pet/app/modules/agendamentos/agendamentos_page.dart';

main() {
  testWidgets('AgendamentosPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(AgendamentosPage(title: 'Agendamentos')));
    //  final titleFinder = find.text('Agendamentos');
    //  expect(titleFinder, findsOneWidget);
  });
}
