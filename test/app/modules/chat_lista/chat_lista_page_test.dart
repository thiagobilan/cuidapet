import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:cuida_pet/app/modules/chat_lista/chat_lista_page.dart';

main() {
  testWidgets('ChatListaPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ChatListaPage(title: 'ChatLista')));
    //  final titleFinder = find.text('ChatLista');
    //  expect(titleFinder, findsOneWidget);
  });
}
