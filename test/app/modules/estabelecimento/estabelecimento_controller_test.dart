import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:cuida_pet/app/modules/estabelecimento/estabelecimento_controller.dart';
import 'package:cuida_pet/app/modules/estabelecimento/estabelecimento_module.dart';

void main() {
  initModule(EstabelecimentoModule());
  // EstabelecimentoController estabelecimento;
  //
  setUp(() {
    //     estabelecimento = EstabelecimentoModule.to.get<EstabelecimentoController>();
  });

  group('EstabelecimentoController Test', () {
    //   test("First Test", () {
    //     expect(estabelecimento, isInstanceOf<EstabelecimentoController>());
    //   });

    //   test("Set Value", () {
    //     expect(estabelecimento.value, equals(0));
    //     estabelecimento.increment();
    //     expect(estabelecimento.value, equals(1));
    //   });
  });
}
