import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:cuida_pet/app/modules/home/endereco/endereco_page.dart';

main() {
  testWidgets('EnderecoPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(EnderecoPage(title: 'Endereco')));
    //  final titleFinder = find.text('Endereco');
    //  expect(titleFinder, findsOneWidget);
  });
}
